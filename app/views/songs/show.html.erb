<h1>Song</h1>

<p>
  <strong>Name:</strong>
  <%= @song.name %>
</p>

<p>
  <strong>Band:</strong>
  <%= @song.band.name %>
</p>

<p>
  <strong>Genre:</strong>
  <%= @song.genre %>
</p>


<%= link_to 'Back', band_path(@song.band.id), class: "btn btn-default btn-lg" %>
<%= link_to 'Edit', edit_song_path(@song), class: "btn btn-default btn-lg" %>
</div>